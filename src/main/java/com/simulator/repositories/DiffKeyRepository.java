package com.simulator.repositories;

import com.simulator.model.DiffKey;
import org.springframework.data.repository.CrudRepository;

public interface DiffKeyRepository extends CrudRepository<DiffKey, Long> {
}
